import type { Model } from '../client/interfaces/Model';

const models: Model[] = [
    {
        export: 'interface',
        name: 'MyModel',
        type: 'MyModel',
        base: 'MyModel',
        template: null,
        link: null,
        description: null,
        isDefinition: true,
        isReadOnly: false,
        isRequired: false,
        isNullable: false,
        imports: [],
        extends: [],
        enum: [],
        enums: [],
        properties: [],
    },
];

describe('dateTypeOverride', () => {
    it('sucess', async () => {

    });
});
